import React from 'react'

// Given an array of strings, create links and routes. E.g. "hello" should create a link to a page called /hello
const Nav = () => {
  return (
    <div></div>
  )
}

export default Nav